// main.js
// Entry point: can load modules, set up initial state if needed.

import './ui.js';
// ui.js sets up event listeners and handles the logic when files are chosen.
// main.js could also initialize any global states or run startup code if needed.
